import React from 'react';
import Modal from '../CreateStreamModal';

const StreamDelete = () => {
    return (
        <div>
            <div>StreamDelete</div>
            <Modal />
        </div>
    );
}

export default StreamDelete;